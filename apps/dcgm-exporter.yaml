apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dcgm-exporter
  namespace: argocd
spec:
  project: ai-platform
  source:
    repoURL: https://github.com/forterro/ai-workstation-platform.git
    targetRevision: main
    path: umbrella-charts/gpu/dcgm-exporter
  destination:
    server: https://kubernetes.default.svc
    namespace: dcgm-exporter
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
