apiVersion: v2
name: ollama
description: Ollama umbrella for the AI workstation platform (dynamic multi-model serving)
type: application
version: 0.1.0
appVersion: 0.1.0
dependencies:
  # renovate: registryUrl=https://otwld.github.io/ollama-helm
  - name: ollama
    repository: https://otwld.github.io/ollama-helm/
    version: 1.68.0
    # On/off switch for the Ollama serving engine (see values.yaml `ollama.enabled`).
    condition: ollama.enabled
