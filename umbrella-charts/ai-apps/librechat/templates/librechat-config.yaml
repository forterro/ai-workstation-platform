apiVersion: v1
kind: ConfigMap
metadata:
  name: librechat-config
  labels:
    app.kubernetes.io/name: librechat
data:
  # Rendered verbatim from .Values.librechat.configYaml and mounted at
  # /app/librechat.yaml (CONFIG_PATH). The public chart ships only a minimal
  # placeholder; each workstation supplies its endpoints, MCP servers and model
  # specs through values/librechat.yaml in its private config repo.
  librechat.yaml: |
{{ .Values.librechat.configYaml | indent 4 }}
