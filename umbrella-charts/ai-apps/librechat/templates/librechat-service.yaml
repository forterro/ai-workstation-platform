apiVersion: v1
kind: Service
metadata:
  name: librechat
  labels:
    app.kubernetes.io/name: librechat
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: librechat
  ports:
    - name: http
      port: {{ .Values.librechat.port }}
      targetPort: http
